name: Integração Contínua

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      run_lint:
        description: "Executar Lint"
        required: true
        type: boolean
        default: true
      run_tests:
        description: "Executar Testes"
        required: true
        type: boolean
        default: true

jobs:
  chamar-workflow-lint-e-test:
    uses: ./.github/workflows/lint-and-test.yml
    with:
      run_lint: ${{ inputs.run_lint }}
      run_tests: ${{ inputs.run_tests }}